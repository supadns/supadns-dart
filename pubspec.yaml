name: supadns
description: Bypass blocked DNS for Supabase using DNS-over-HTTPS
version: 1.0.0

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  http: ^1.2.0
  supabase: ^2.0.0

dev_dependencies:
  test: ^1.25.0
